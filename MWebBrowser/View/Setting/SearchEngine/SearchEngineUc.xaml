<UserControl x:Class="MWebBrowser.View.Setting.SearchEngine.SearchEngineUc"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:Cys_CustomControls.Controls;assembly=Cys_CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="20,0,0,0">
            <TextBlock Text="默认搜索引擎：" VerticalAlignment="Center"/>
            <controls:MComboBox VerticalAlignment="Center" Margin="5,0,0,0" ItemsSource="{Binding SearchEngineItemInfos}" DisplayMemberPath="ChineseName" SelectedItem="{Binding SelectedItem}"/>
            <controls:MButton Content="设为默认" Margin="10,0,0,0" StyleType="Success" Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
