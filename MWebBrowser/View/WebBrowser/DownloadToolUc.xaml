<UserControl x:Class="MWebBrowser.View.WebBrowser.DownloadToolUc"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">
    <Grid Background="{DynamicResource WebBrowserBrushes.DownloadToolBackground}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">

            </Grid>
            <Grid Grid.Column="1">

            </Grid>
            <Button Grid.Column="2" Style="{DynamicResource DownloadLookAllButton}" Content="全部显示"/>
            <Button Grid.Column="3" Style="{DynamicResource DownloadCloseButton}" Margin="5,0"/>
        </Grid>
    </Grid>
</UserControl>
