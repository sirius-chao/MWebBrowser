<UserControl x:Class="MWebBrowser.View.WebBrowser.WebMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:attached="clr-namespace:Cys_Resource.Code;assembly=Cys_Resource"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="320" Height="300" Width="320" Background="{DynamicResource WebBrowserBrushes.WebMenuBackground}">
    <UserControl.Resources>
        <Style TargetType="{x:Type MenuItem}" BasedOn="{StaticResource DefaultMenuItem}"/>
        <Style TargetType="{x:Type Menu}" BasedOn="{StaticResource DefaultMenu}"/>
    </UserControl.Resources>
    <Grid>
        <StackPanel>
            <MenuItem Style="{StaticResource TransparentHeaderMenuItem}">
                <MenuItem Header="新建标签页"/>
                <MenuItem Header="新建窗口"/>
            </MenuItem>
        </StackPanel>
    </Grid>
</UserControl>
